package benyon_lab3;

import java.util.Random;

public class GuessGame {
	private int actualNumber;
	private int guesses;
	private int rangeMaximum;
	private int rangeMinimum;

	public GuessGame(int ActualNumber, int guesses, int RangeMinimum,
			int RangeMaximum) {
		this.actualNumber = ActualNumber;
		this.guesses = guesses;	
		this.rangeMinimum = RangeMinimum;
		this.rangeMaximum = RangeMaximum;
	}
	
	public GuessGame() {
		this(0,0,0,100);
	}
	
	public void displayStatistics() {
		System.out.println(guesses);
	}
	
	public void newTarget() {
		Random rand = new Random();
		actualNumber = rangeMinimum + rand.nextInt(rangeMaximum - rangeMinimum);
	}
	
	public boolean Guess(int num) {
		displayHint(num);
		return num == this.actualNumber;
	}
	
	public int getRangeMinimum() {
		return rangeMinimum;
	}
	
	public int getRangeMaximum() {
		return rangeMaximum;
	}
	
	public void checkAnswer() {
		System.out.println(actualNumber);
	}
	
	private void displayHint(int num) {
		if(num > actualNumber){
			System.out.printf("%d is too high\n", num);
		} else if(num < actualNumber){
			System.out.printf("%d is too high\n", num);
		}
	}
	
}
